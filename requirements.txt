# Phase 2 ingestion deps. Install into a fresh venv:
#   python -m venv .venv
#   .venv\Scripts\activate     (Windows PowerShell: .\.venv\Scripts\Activate.ps1)
#   pip install -r requirements.txt
#
# .venv/ is gitignored. Will move to a separate sibling repo when we deploy
# the ingestion service to Railway/Render.

yfinance>=0.2.50,<0.3
psycopg[binary]>=3.2,<4
python-dotenv>=1.0,<2
cuid>=0.4,<0.5
pandas>=2.0,<3
